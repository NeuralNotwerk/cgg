;; c.tsg — placeholder stack-graphs rules for C.
;;
;; Holds the architectural slot for a future scope-graph-based C
;; resolver. The bulk of C cross-file resolution is done by
;; `cgg_resolve::cross_file` consuming `#include` import records
;; emitted by the extractor.
;;
;; What a future real `c.tsg` would add: struct member access,
;; function pointer dispatch, typedef resolution.

(translation_unit) @_root {
}
