;; csharp.tsg — placeholder stack-graphs rules for C#.
;;
;; Holds the architectural slot for a future real C# scope graph.
;; Cross-namespace resolution is currently handled by the
;; `cgg_resolve::cross_file` resolver using the `using` directives
;; emitted by the extractor.
;;
;; Full C# scope semantics needed later: partial classes,
;; inheritance chains, explicit interface implementations,
;; `using static`, extension methods.

(compilation_unit) @_root {
}
